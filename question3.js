3. // Log the last element of the array/list of length 6.

    const list=["1","2","3","4","5","6"]
     console.log(list.length);        // length of the list.
     console.log( list.slice(-1));   // last element of the array.
