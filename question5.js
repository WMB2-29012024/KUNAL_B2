5. // Use pop method to remove last element in the array, and log the removed element. Also log the length after removal of element.

    let arr=[11,22,33]
     console.log( arr.length);  // Before removing element
     arr.pop();
     console.log(arr.pop());    // after remove element
     console.log(arr.lenth);